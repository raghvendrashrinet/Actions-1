name: First workflow

on: push

jobs:
    first job:
        runs-on: ubuntu-latest 
        steps:
            - name : welcome message
              run: echo " my fist github action job"

            - name : list files
              run: ls

            - name: read file
              run: cat README.md 

          